

graph TD
    subgraph 电源输入
        A[USB 5V]
    end

    subgraph 电池管理
        A --> B{充电IC};
        B --> C[锂离子电池];
        C --> D{保护IC};
    end

    subgraph 系统供电
        D --> E[TDA2822 功放];
        D --> F{3.3V 升降压转换器};
        D --> G[分压电路];
    end

    subgraph 稳压输出
        F --> H[3.3V 系统电压轨];
        H --> I[RP2040];
        H --> J[ST7789];
        H --> K[INMP441];
        H --> L{1.8V LDO};
        H --> M{2.8V LDO};
    end

    subgraph 摄像头供电
        L --> N[OV7670 数字核心];
        M --> O[OV7670 模拟电源];
        H --> P[OV7670 I/O];
    end

    subgraph 监控
        G --> Q[RP2040 ADC 引脚];
    end